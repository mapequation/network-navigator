{
  "name": "statenet",
  "version": "1.0.0",
  "description": "null",
  "license": "ISC",
  "repository": "null",
  "author": "Anton Eriksson",
  "main": "src/app.js",
  "scripts": {
    "build": "browserify -t [ babelify --presets [ env ] ] src/app.js -o public/js/bundle.js",
    "build:watch": "watch 'npm run build' src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "d3": "^4.12.2"
  },
  "devDependencies": {
    "babel-core": "latest",
    "babel-preset-env": "latest",
    "babelify": "latest",
    "browserify": "latest",
    "watch": "latest"
  }
}
